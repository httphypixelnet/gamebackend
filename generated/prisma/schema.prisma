generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma"
}

datasource db {
  provider = "sqlite"
  url      = "file:../db.sqlite"
}

model User {
  id    String @id @default(uuid())
  score Int    @default(0)
  name  String
}
